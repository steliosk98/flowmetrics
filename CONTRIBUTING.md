# Contributing

Use Node 22, keep changes focused, and run `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build:docker`. Add tests for calculations, timezones, event transitions, and connector mappings. Never commit credentials or real device payloads. Use Conventional Commits and explain migrations or data-quality changes in pull requests.
